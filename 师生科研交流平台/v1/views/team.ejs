<!DOCTYPE html>
<html>

<head>
    <title>
        <%=team.name%> - 团队资料 - 师生科研互动平台</title>
    <%include parts/include.css.html%>
</head>

<body>
    <% include parts/nav.ejs %>
        <input type="hidden" value="<%=team._id%>" id="team-_id">
        <div class="container mt20">
            <div class="row">
                <div class="col-xs-12">
                    <div class="page-header">
                        <h1><%=team.name %></h1>
                        <h5 class="text-muted"><i class="fa fa-clock-o"></i>&nbsp;创建于<%=team.date %><i class="fa fa-user ml20"></i>&nbsp;负责人：<%=team.leader.name %></h5>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-9 col-xs-12">

                    <fieldset>
                        <legend>参与项目</legend>
                        <div id="projects">
                            <p class="text-muted" id="load-state">加载中</p>
                        </div>
                    </fieldset>
                </div>
                <div class="col-sm-3 col-xs-12">
                    <div class="aside aside-right">
                        <%if(team.leader._id.toString()!==user._id.toString() ){%>

                            <button type="button" class="mr20 mb10 btn btn-danger <%=isSelected?'':'dhidden'%>" id="btn-submit-report">退出</button>
                            <button type="button" class="mr20 mb10 btn btn-warning <%=isApplied?'':'dhidden'%>" id="btn-cancel-select">取消申请</button>
                            <button type="button" class="mr20 mb10 btn btn-primary <%=(isApplied||isSelected)?'dhidden':''%>" id="btn-apply">申请加入</button>

                            <% } else{%>

                                <button type="button" class="mr20 mb10 btn btn-danger" id="btn-update-content" data-toggle="modal" data-target="#edit-project">解散</button>

                                <%}%>
                                    <hr/>
                                    <div class="text-muted">成员：</div>
                                    <div id="team-members" class="mt10">
                                        <span class="text-muted">加载中</span>
                                    </div>
                    </div>
                </div>
            </div>
        </div>
        <% include parts/include.js.html %>
            <script type="text/javascript" src="/js/team.js"></script>
</body>

</html>